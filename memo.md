⚠️ cachyos-fish-config     (fish 使ってないなら不要) => fish使っていない
⚠️ cachyos-zsh-config      (sheldon+starship と競合する可能性) => 内容しらないため消しづらい
⚠️ cachyos-micro-settings  (micro 使ってないなら不要) => microってなに？
⚠️ cachyos-hello           (初期セットアップ UI、不要) => 消していいかわからない
⚠️ cachyos-wallpapers      (自前壁紙使うなら不要) => 使わない
⚠️ cachyos-themes-sddm     (astronaut 使うなら不要) => 使わない
⚠️ cachyos-grub-theme      (好み) => これはなに？windowsの設定してないやそういえば
⚠️ cachyos-plymouth-theme, cachyos-plymouth-bootanimation (好み) => plymouthってなに? 

ターミナル
alacritty => 使っていない
kitty => ghosttyではなく気分転換で使うときある

ランチャー
hyprlauncher => 使っていない
wofi => 使っていない

エディタ
micro -> 多分いらない
nano, vim, => いる

システムモニター
btop, fastfetch, glances => 使ったことない、見たこと無いで判断つきづらい

ブラウザ
firefox => zen browserのフェール用として必要

その他
dolphin => カスタムしずらいため、変えるよてい, 変えた場合GUI系のアプリなので別のパッケージマネージャー使うか悩む
meld => つかったことない
discord => つかっているけどpacmanで持つべきか
shelly => なぞ

他
hyprlock入れる
pacman, yayでいれているパッケージリストは更新、dotfilesに残せるのか
chezmoiのインスロール時は何のパッケージマネージャーを使うのか
sheldon が公式リポジトリにあるか要確認 => 確認したらありました。
walker, elephantはそれ単体だけでなく他の依存モジュールが必要だった気がする


cachyos-grub-theme => windwosとデュアルブートしているためできるなら残したい, windows設定をしていないため対応必須, backlog
plymouth => ブート時のアニメーションなら残しておきたい
shelly => 削除OK


