// Sync dotfiles package snapshot (user additions only, baseline excluded)
// Deploy to: /etc/apt/apt.conf.d/99sync-user-packages
DPkg::Post-Invoke { "sudo -u mkiin /home/mkiin/dotfiles/scripts/sync-packages-apt.sh >/dev/null 2>&1 || true"; };
