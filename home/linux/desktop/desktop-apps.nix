{
  programs.vesktop = {
    enable = true;

    settings = {
      tray = true;
      minimizeToTray = true;
      hardwareAcceleration = true;
      arRPC = true;
    };

    # vencord.settings = {
    #   # plugins = { ... };
    # };
  };
}
