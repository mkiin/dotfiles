[Manager]
# systemd user manager の全子プロセスに継承される PATH。
# Hyprland を uwsm 経由で起動するため、ここに書いた PATH が
# Hyprland 自身と exec-once 子プロセスすべてに行き渡る。
# ~/.local/bin を含めるために明示。残りの /usr/bin 等は systemd 既定でも入るが念のため列挙。
DefaultEnvironment=PATH=%h/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
