name: "CI: Nix diff"

on:
  pull_request:
    paths:
      - "flake.lock"
      - "flake.nix"
      - "hosts/**"
      - "lib/**"
      - "nixos/**"
      - "home-manager/**"
      - "packages/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  nix-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Nix
        uses: ./.github/actions/setup-nix

      - name: Run nix-diff
        uses: natsukium/nix-diff-action@v1.1.1
        with:
          skip-no-change: true
          # "mkiin@wsl" は '@' を含み attribute regex を通らないため nixos のみ
          attributes: |
            - displayName: nixos
              attribute: nixosConfigurations.nixos.config.system.build.toplevel
